import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  deleteContainer: {padding: 16, alignItems: 'center'},
  deleteText: {fontSize: 16, marginBottom: 10, color: '#333'},
});

export default styles;
